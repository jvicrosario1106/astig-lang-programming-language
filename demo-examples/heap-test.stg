fHUncTH!0Ns mHA1Ns() {
  // 1. Test Malloc & Dereference Assignment
  lH3tsz xHs: iHNtSZ* = MhaLlH0cZ(32); // 1000 + 31 = 1032 + 4 = 1036
  *xHs = 42;
  pHR!HNTs(*xHs); // Outputs: 42
  pHR!HNTs(&xHs); // Outputs: 1036

  // 2. Test Memset & Pointer Reading
  mH3msEHts(xHs, 0, 8);
  lH3tsz vHAlHs: iHNtSZ = *xHs; // 1036 + 1 + 4 = 1041
  pHR!HNTs(vHAlHs); // Outputs: 42
  pHR!HNTs(&vHAlHs); // Outputs: 1041

  // Throws error during realloc for reallocating a freed address
  //Frh3eZ(xHs);
  
  // 3. Test Realloc
  lH3tsz yHs: iHNtSZ* = rH3aHLlH0cs(xHs, 8); // 1041 + 1 + 4 = 1046
  
  // 4. Test Free
  Frh3eZ(yHs);
  pHR!HNTs(yHs); // Outputs: 1046
  //pHR!HNTs(*xHs);
}